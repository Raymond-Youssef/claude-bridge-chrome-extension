<svg xmlns="http://www.w3.org/2000/svg" width="128" height="128" viewBox="0 0 128 128">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#1a1410"/>
      <stop offset="100%" stop-color="#2e2419"/>
    </linearGradient>
    <linearGradient id="accent" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#e89050"/>
      <stop offset="100%" stop-color="#c75b24"/>
    </linearGradient>
  </defs>

  <!-- Rounded square background -->
  <rect width="128" height="128" rx="28" ry="28" fill="url(#bg)"/>

  <!-- Subtle inner border -->
  <rect x="2" y="2" width="124" height="124" rx="26" ry="26" fill="none" stroke="#4a3828" stroke-width="1.5" opacity="0.6"/>

  <!-- Crosshair ring -->
  <circle cx="64" cy="64" r="26" fill="none" stroke="url(#accent)" stroke-width="4.5"/>

  <!-- Center dot -->
  <circle cx="64" cy="64" r="5" fill="#e07a3a"/>

  <!-- Crosshair lines - extending from ring outward -->
  <!-- Top -->
  <line x1="64" y1="18" x2="64" y2="34" stroke="url(#accent)" stroke-width="4.5" stroke-linecap="round"/>
  <!-- Bottom -->
  <line x1="64" y1="94" x2="64" y2="110" stroke="url(#accent)" stroke-width="4.5" stroke-linecap="round"/>
  <!-- Left -->
  <line x1="18" y1="64" x2="34" y2="64" stroke="url(#accent)" stroke-width="4.5" stroke-linecap="round"/>
  <!-- Right -->
  <line x1="94" y1="64" x2="110" y2="64" stroke="url(#accent)" stroke-width="4.5" stroke-linecap="round"/>

  <!-- Small angle brackets < > to suggest code/React -->
  <!-- Left bracket < -->
  <polyline points="44,52 36,64 44,76" fill="none" stroke="#d4956a" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" opacity="0.85"/>
  <!-- Right bracket > -->
  <polyline points="84,52 92,64 84,76" fill="none" stroke="#d4956a" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" opacity="0.85"/>
</svg>
